流程：
	1、创建数据，得到数据：得到一个脏数据集，数据格式多种多样
	2、数据准备(清洗数据,可能要重复，返工)
		1、花费时间最长的一个步骤
		2、对分析结果产生重大影响，清洗数据做法：
			1、感受数据
				相关方法：describe()|head()|tail()|单独取某一列（df.columns_name）
			2、空值处理
				删除空值：
					适用场景：
						所占比重小，对分析结果不产生很大影响
						dropna（）
						通过布尔索引，索引来源,isnull(),notnull()
				填充：
					填充方式：
						按照平均值来填
						随机填
						其他
						原则就是尽量还原真实场景，让分析更准确
						fillna()
			3、离群值处理
				删除
				裁剪
				where
	3、分析数据（取相关列）：
		1、想分析什么就去哪些列
			具体方法就是那些统计函数：mean()|max()|min()
		2、多列的：groupby：groupby完成对了之后再用那些统计函数